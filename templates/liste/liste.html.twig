    <style>
    table, th, td {
  border: 1px solid;
}
    </style>

<h1>Liste complète des marque-pages</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">URL</th>
      <th scope="col">Commentaire</th>
    </tr>
  </thead>
  <tbody>
    {% for marquePage in marquePage %}
      <tr>
        <td><a href="/marquepage/consulter/{{ marquePage.getId() }}"> {{ marquePage.getUrl() }}</a></td>
        <td>{{ marquePage.getCommentaire() }}</td>
      </tr>
    {% endfor %}
  </tbody>
</table>

<a href="/marquepage/ajouter">Ajouter marque page !</a>
